function tenTimes(number) {
    const result = number * 10;
    return result;
}

const outPut = tenTimes(99);
console.log('The OutPut Is : ', outPut);


// 

function divied(num) {
    const half = num / 2;
    return half;
}
const diviedNumber = divied(78);
console.log('Divied Numer ', diviedNumber);
